/** Everywhere Spiral Clean can take you. The rail that used to render these is
 *  gone — every Spiral app wears the same bar now, and the bar holds the menu —
 *  so the list lives with the app's other vocabulary rather than in a component
 *  named after a thing that no longer exists. */
export type Destination =
  | "clean"
  | "storage"
  | "optimize"
  | "uninstall"
  | "history"
  | "settings";
