# Default uninstall associations

Spiral Clean will select both verified and likely app associations by default to reduce manual cleanup work. Likely associations must remain visibly labeled and independently deselectable in a mandatory final review that shows every selected item and its size, because their evidence is weaker than an exact system-level match.
