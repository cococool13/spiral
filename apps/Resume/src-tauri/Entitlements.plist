<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- llama-server (the offline engine) maps model weights and, on Apple
	     silicon, compiles Metal kernels. Hardened Runtime blocks both unless
	     these are set. Library validation is off because the sidecar loads
	     system Metal/Accelerate; without this, a notarized build reports the
	     offline engine missing even though the binary is in the bundle. -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
</dict>
</plist>
