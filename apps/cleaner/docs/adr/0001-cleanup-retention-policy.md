# Cleanup retention policy

Spiral Cleaner will permanently remove only user-selected safe categories after an explicit confirmation. All other cleanup candidates use recoverable cleanup by default, because the product must reclaim space without treating uncertain files as disposable.
