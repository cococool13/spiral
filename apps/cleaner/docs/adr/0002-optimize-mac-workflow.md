# Optimize Mac workflow

Spiral Cleaner will offer one Optimize Mac workflow because users need a convenient maintenance entry point. It will generate a reviewable plan of individually named actions, obtain administrator permission only for actions that need it, provide per-action opt-outs, and report successes, skips, and failures rather than presenting an opaque promise of faster performance.
