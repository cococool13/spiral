# Keeps the default gitleaks rules and allowlists one string they misread.
#
# `BraveP3AEnabled` is a Brave enterprise policy key — a public Chromium
# identifier that SlimBrave writes into managed-policy tables, and one of
# several hundred such names in this repo. The generic-api-key rule matches it
# because of what sits beside it — `Key = "..."` in the PowerShell table and
# `"key": "..."` in the Python ones — which is what a leaked credential also
# looks like. Six findings, all this one string, all in policy tables.
#
# It is allowlisted by exact secret value rather than by path: allowlisting
# apps/slim/** would stop scanning a directory that writes system policy and
# shells out with elevation, which is the last place worth going quiet. A real
# key committed beside these tables still fails.
#
# regexTarget is stated rather than left to default, so an upgrade that changes
# the default cannot silently widen what this excuses. The regex targets the
# extracted secret and is anchored, so it covers this exact value and nothing
# else. Do not switch it to "match": that compares against the rule's whole
# matched span, which for generic-api-key is wider than the value itself.
#
# Checked by planting a high-entropy ghp_ token in the same policy table:
# without this file gitleaks reports the token plus three BraveP3AEnabled
# findings, with it only the token. Entropy matters when repeating that test —
# a placeholder like ghp_ followed by 36 identical characters is filtered
# before any rule sees it, and reads as a pass that never ran.
#
# If a future policy name trips the same rule, add it here with the same
# reasoning — do not widen this to a path, to "match", or drop the anchors.

title = "Spiral"

[extend]
useDefault = true

# Two separate allowlists, not one with both keys. Conditions inside a single
# allowlist can be combined, and a value-plus-path allowlist that AND-ed them
# would excuse the policy name only inside build output — which is the one
# place it never appears.
[[allowlists]]
description = "Public Brave/Chromium enterprise policy key names, not credentials"
regexTarget = "secret"
regexes = [
  '''^BraveP3AEnabled$''',
]

# Generated trees, excluded by path rather than by value.
#
# `gitleaks detect` over history never sees these — they are all gitignored.
# `--no-git`, which is what a pre-commit hook or a local sweep tends to run,
# walks the working directory instead and does not read .gitignore, so it
# reports whatever a build left behind: Rust .rmeta in target/deps, and Next's
# prerender-manifest.json and .next/cache preview tokens, which are minted per
# build. Twenty-two findings, none of them committable.
#
# Excluding by path is safe here in a way it would not be for source: nothing
# under these directories is authored or committed, so there is no secret they
# could hide. .claude/worktrees is listed because a worktree is a second copy
# of this repo — without it every finding is counted once per worktree.
[[allowlists]]
description = "Generated and vendored trees — nothing here is authored or committed"
paths = [
  '''(^|/)node_modules/''',
  '''(^|/)target/''',
  '''(^|/)\.next/''',
  '''(^|/)out/''',
  '''(^|/)dist/''',
  '''(^|/)\.claude/worktrees/''',
]
